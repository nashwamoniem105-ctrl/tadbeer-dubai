# تقرير تحليل وتشخيص مشاكل مستودع (tadbeer-dubai)

يقدم هذا التقرير تحليلاً تقنياً شاملاً ومفصلاً للأسباب الجذرية للمشاكل الثلاث التي واجهتها في مستودع مشروع **tadbeer-dubai** على GitHub، مع تقديم الحلول البرمجية المقترحة لكل مشكلة لضمان استقرار النظام وتجربة مستخدم سلسة.

---

## أولاً: مشكلة حذف البيانات مباشرة من لوحة التحكم (Admin Dashboard)

### 1. الوصف والتشخيص
عند فحص ملف الخادم الخلفي (`server.js`)، تبين أن دالة تهيئة قاعدة البيانات (`initDb`) تحتوي على أمر صريح ومباشر لحذف جميع السجلات القديمة تلقائياً في كل مرة يتم فيها تشغيل الخادم أو إعادة تشغيله:

```javascript
db.exec(`DELETE FROM leads; DELETE FROM payments; DELETE FROM users; DELETE FROM requests; DELETE FROM contacts;`);
```

أو في نسخة PostgreSQL:
```javascript
await db.query(`DELETE FROM leads; DELETE FROM payments; DELETE FROM users; DELETE FROM requests; DELETE FROM contacts;`);
```

**السبب الجذري:**
تم تصميم هذه الأوامر في الأصل كبيانات تجريبية (Demo seed/cleanup) أثناء التطوير والاختبار المحلي، بحيث يتم تفريغ الجداول مع كل إعادة تشغيل للخادم (Server Restart). ونظراً لأن منصات الاستضافة السحابية (مثل Railway أو Render) تقوم بإعادة تشغيل الحاوية (Container Restart) أو إيقافها مؤقتاً عند الخمول، فإن إعادة التشغيل هذه تؤدي في كل مرة إلى تنفيذ أمر الحذف (`DELETE`)، مما يتسبب في مسح جميع البيانات المدخلة من لوحة التحكم والعملاء فوراً.

### 2. الحل المقترح
يجب إزالة أو التعليق على أسطر حذف البيانات تلقائياً (`DELETE FROM...`) داخل دالة `initDb` في ملف `server.js`، بحيث تقتصر دالة التهيئة على إنشاء الجداول إذا لم تكن موجودة (`CREATE TABLE IF NOT EXISTS`) فقط دون المسح المتعمد للبيانات الموجودة:

```javascript
// تم إزالة أسطر الحذف التلقائي من دالة initDb لمنع مسح بيانات العملاء والمدفوعات عند إعادة تشغيل الخادم
```

---

## ثانياً وثالثاً: مشكلة إعادة التوجيه إلى صفحة الدفع عند تحديث صفحتين (OTP) أو (ATM Pin)

### 1. الوصف والتشخيص
عندما يكون المستخدم في صفحة إدخال رمز التحقق (`OTP`) أو صفحة إدخال رقم سري البطاقة (`ATM Pin`) ويقوم بتحديث الصفحة (Refresh)، يتم إرجاعه مباشرة إلى صفحة الدفع الرئيسية (`payment-gateway.html`).

**السبب الجذري:**
1. **الاعتماد الكلي على حالة الجلسة المحلية (SessionStorage):** يعتمد تطبيق الواجهة الأمامية في ملف `payment-gateway.html` على إدارة المراحل (States) داخل نفس الصفحة الواحدة باستخدام عناصر مخفية ومرئية (`pg-card`, `pg-otp`, `pg-atm`, `pg-success`). عند حدوث إعادة تحديث للصفحة (Page Refresh)، يتم إعادة تحميل الملف بالكامل من البداية، ولا يقوم الكود الحالي بالتحقق من المرحلة النشطة السابقة للمستخدم (مثل تخزين المرحلة الحالية في `sessionStorage`).
2. **غياب منطق تتبع المرحلة (State Persistence):** لا يتم حفظ الخطوة التي وصل إليها المستخدم (سواء كانت بطاقة، أو OTP، أو PIN) في متصفح العميل، مما يجعل التطبيق يفترض دائماً أن المستخدم في الخطوة الأولى (إدخال بيانات البطاقة).

### 2. الحل المقترح
لحل هذه المشكلة وجعل واجهة الدفع قادرة على استعادة المرحلة الحالية للمستخدم عند تحديث الصفحة، يجب تخزين واسترجاع المرحلة النشطة (`currentStage`) باستخدام `sessionStorage`:

1. عند الانتقال بين المراحل (مثل الانتقال إلى OTP أو PIN)، يتم حفظ المرحلة الحالية:
   ```javascript
   sessionStorage.setItem('tdPaymentStage', 'otp'); // أو 'pin'
   ```
2. عند تحميل الصفحة (`DOMContentLoaded` أو عند بدء تنفيذ السكريبت)، يتم قراءة المرحلة المخزنة وعرض العنصر المماثل لها مباشرة بدلاً من العودة الافتراضية للبطاقة:
   ```javascript
   const savedStage = sessionStorage.getItem('tdPaymentStage') || 'card';
   if (savedStage === 'otp') show('pg-otp');
   else if (savedStage === 'pin') show('pg-atm');
   else show('pg-card');
   ```

---

## جدول تلخيصي للمشاكل والحلول

| المشكلة | السبب الجذري في الكود | الحل البرمجي |
| :--- | :--- | :--- |
| **حذف البيانات في لوحة Admin** | وجود أوامر `DELETE FROM` داخل دالة تهيئة قاعدة البيانات `initDb` في `server.js`. | حذف أو تعطيل أوامر الحذف التلقائي عند بدء تشغيل الخادم. |
| **إعادة التوجيه عند تحديث OTP / PIN** | عدم حفظ خطوة الدفع الحالية وافتراض البدء من جديد (`pg-card`) عند كل تحديث للصفحة. | تخزين واسترجاع المرحلة الحالية باستخدام `sessionStorage` لإعادة توجيه المستخدم لنفس الخطوة عند التحديث. |

---
**إعداد:** فريق التطوير الفني
